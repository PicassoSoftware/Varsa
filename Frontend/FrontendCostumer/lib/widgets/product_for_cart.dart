import 'package:flutter/material.dart';

class ProductForCard extends StatelessWidget {
  const ProductForCard({Key? key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return Card(
      child: Column(children: [],),
    );
  }
}